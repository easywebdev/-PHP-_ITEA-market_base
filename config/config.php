<?php
class Config {
    var $BASE_URL = 'market.loc';  // Site Base URL
    var $DB_HOST = 'localhost';    // Data Base address
    var $DB_NAME = 'market';       // Data Base name
    var $DB_USER = 'root';         // Data Base login
    var $DB_PASS = '';             // Data Base password
}

$config = new Config();
?>